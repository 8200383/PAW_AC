class PurchaseController {
    create(req, res, next) {
        res.json({ status: 'added' })
    }

    getAll(req, res, next) {
        res.json({ status: 'added' })
    }

    get(req, res, next) {
        res.json({ status: 'added' })
    }

    update(req, res, next) {
        res.json({ status: 'added' })
    }
}

module.exports = new PurchaseController()
